<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title><%= title %></title>
</head>
<body>
<% for (let i = 0; i < messages.length; i++) { %>
  <li>
    <div class="message">
      <div>Name: <%= messages[i].username %></div> 
      <div>Date: <%= messages[i].added.toLocaleString() %></div>
      <div>Message: <%= messages[i].text %></div>
  </div>
  <a href="/messages/<%= messages[i].id %>">Open</a>
  </li>
  <hr>
  <% } %>
  <a href="/new">Add a new Message</a>
</body>
</html>